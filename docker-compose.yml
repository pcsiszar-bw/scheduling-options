services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=scheduled_tasks'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
